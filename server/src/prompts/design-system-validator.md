# design-system-validator prompt (reference)

This validator's live system prompt is defined inline in
`server/src/validators/`. This file is a pointer for anyone browsing the
knowledge/prompts folder rather than the source — keeps the prompt next to
the tool calls it governs so they can't drift out of sync.
